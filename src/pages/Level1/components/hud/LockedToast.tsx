// Locked-interaction feedback is now delivered by the ARIA guide character
// (GuideCharacter.tsx), which reads `lockedToastVisible` from the game store.
// This component is intentionally kept but renders nothing.
export default function LockedToast() {
  return null
}
